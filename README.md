# Switchyard releases

Run several Claude Code accounts on one Linux or WSL machine, each with its own sign-in, settings and sessions, and start the right one by folder.

This repository holds only the released binaries, the installer and the release notes. The source code is kept private. A full README with install steps follows with the first release.
